Implemented Spring Security with JWT authentication, allowing only one active session per user.
Logging in from a new device revokes the previous session. Secure authentication, refresh tokens, and logout handling ensure robust security.
